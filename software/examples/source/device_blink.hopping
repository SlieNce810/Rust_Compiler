func int main() {
    int counter;
    int limit;
    int led;

    counter = 0;
    limit = 200000;
    led = 0;

    while (counter < limit) {
        counter = counter + 1;
    }

    if (counter == limit) {
        led = 1;
    } else {
        led = 0;
    }

    return led;
}
