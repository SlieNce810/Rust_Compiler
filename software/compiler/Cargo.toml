[package]
name = "mini_embedded_compiler"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.13.2", default-features = false, features = ["blocking", "json", "rustls"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
